# name: Daily scraping run

# on:
#   schedule:
#     - cron: "0 15 * * *"  # daily at 10 AM EST (3 PM UTC)
#   workflow_dispatch:

# jobs:
#   run:
#     runs-on: ubuntu-latest
#     env:
#       MONGO_URI: ${{ secrets.MONGO_URI }}
#       DB_NAME: ${{ vars.DB_NAME }}
#       USE_HEADLESS: ${{ vars.USE_HEADLESS }}
#       USE_VERBOSE: ${{ vars.USE_VERBOSE }}
#       REQUEST_TIMEOUT: ${{ vars.REQUEST_TIMEOUT }}
#       SCRAPER_OUTPUT_DIR: ${{ vars.SCRAPER_OUTPUT_DIR }}
#       AWS_REGION: us-east-2

#     steps:
#       - uses: actions/checkout@v4

#       - name: Set up Python
#         uses: actions/setup-python@v5
#         with:
#           python-version: "3.11"

#       - name: Install system dependencies
#         run: sudo apt-get install -y xvfb

#       - name: Install Python dependencies and Playwright
#         run: |
#           python -m pip install --upgrade pip
#           pip install -r requirements.txt
#           playwright install chromium

#       - name: Scrape indexes
#         run: xvfb-run -a python scripts/scrape_indexes.py

#       - name: Scrape articles
#         run: xvfb-run -a python scripts/scrape_articles.py