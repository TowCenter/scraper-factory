You are implementing a web scraper for a {{ content_type }} page. {{ content_description }}

I'm providing you with a template that already has the basic structure for a Playwright-based web scraper.

Here is the template code:

```python
{{ rendered_template }}
```

Based on analysis of the page structure, here are the candidate selectors found:

**Item Selectors with HTML Examples (containers for each {{ item_label }}):**
{{ item_examples }}

{% for field in fields %}
**{{ field.name | capitalize }} Selectors with HTML Examples ({{ field.description }}):**
{{ field_examples[field.name] }}

{% endfor %}
**Next Page Selectors with HTML Examples:**
{{ next_page_examples }}

Your task is to analyze the HTML examples above and implement the scraper with intelligent navigation:

**Navigation Strategy:**
1. If next page selectors are found: Use them for traditional page navigation in advance_page(). Prioritize a 'next' or 'load more' button or link over other pagination types.
2. If no next page selectors are found: Implement infinite scroll as fallback in advance_page()

**Implementation Requirements:**
1. Review the HTML examples for each selector and choose the most appropriate ones for extracting the required fields.
2. Implement the scrape_page() function using your chosen selectors.
3. Implement the advance_page() function with this logic:
   - **If next page selectors exist**: Click/navigate to next page using those selectors, or navigate to the next page URL based on hrefs
   - **If no next page selectors exist**: Use infinite scroll fallback (scroll to bottom, wait for new content, return same URL)
4. Return structured data as a list of dictionaries with these keys: {% for field in fields %}{{ field.name }}, {% endfor %}scraper. Always set `scraper` to `SCRAPER_MODULE_PATH` (already defined as a module-level constant in the template).
5. This is an async scraper using Playwright's async API. Make sure to use 'await' for ALL async methods.
{% if has_date_field %}
6. Make sure to handle date formatting consistently (YYYY-MM-DD) for the date field.
{% endif %}

**Fields to extract for each {{ item_label }}:**
{% for field in fields %}
- **{{ field.name }}** ({{ "required" if field.required else "optional" }}): {{ field.description }}
{% endfor %}

**Important Notes:**
- ALWAYS include `'scraper': SCRAPER_MODULE_PATH` in every item dict — this is required for traceability and tests will fail without it.
- DO NOT USE OVERLY SPECIFIC SELECTORS (watch for any that include IDs or strings that look auto-generated)
- Many modern sites use infinite scroll without visible pagination buttons
- Some sites have "Load More" buttons that auto-trigger on scroll
- Always implement a fallback strategy if primary navigation fails
- Test your selectors thoroughly - choose the most reliable ones from the examples
- Ensure scrapers don't break entirely because of malformed HTML or missing elements - use safe access patterns, especially if the item selector returns null or empty lists.
- Be sure to understand child and parent HTML elements when selecting elements, since item containers should be parents of all the child field elements.
- For optional fields, return None if the value is not found rather than raising an error.
- **Use `text_content()` instead of `inner_text()` when reading text from elements that may be inside collapsed containers** (e.g. `<details>`, hidden divs). `inner_text()` returns empty for visually hidden elements; `text_content()` always returns the DOM text regardless of visibility.

CRITICAL: Return ONLY the Python code with appropriate in-line comments and function docstrings. Do not include any explanations, descriptions, or markdown code blocks. Start your response directly with the Python imports and code. No introductory text, no ```python blocks, just the raw Python script that can be executed immediately.
